// Affiliate events: accepts submissions through an API route and displays approved calendar items.
import { useMemo, useState } from 'react';
import Layout from '../components/Layout';
import { readJson, sortByDateTime } from '../lib/data';
export async function getServerSideProps(){ return { props:{ initialEvents: sortByDateTime(readJson('events.json').filter(e=>e.approved)) } }; }
export default function Events({initialEvents}){ const [events,setEvents]=useState(initialEvents); const [form,setForm]=useState({title:'',organizer:'',date:'',time:'',venue:'',category:'Culture'}); const [category,setCategory]=useState('All'); const [message,setMessage]=useState(''); const cats=['All',...new Set(events.map(e=>e.category))]; const filtered=useMemo(()=>events.filter(e=>category==='All'||e.category===category),[events,category]); async function submit(e){e.preventDefault(); const res=await fetch('/api/events',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)}); if(res.ok){setMessage('Submitted for editorial approval.'); setForm({title:'',organizer:'',date:'',time:'',venue:'',category:'Culture'});} } return <Layout title="Affiliate Event Calendar | Africa Unveiled"><section className="section"><p className="eyebrow">Affiliate Event Submission</p><h1>Submit and discover affiliate events</h1>{message&&<p className="notice">{message}</p>}<form className="form" onSubmit={submit}>{['title','organizer','date','time','venue','category'].map(field=><label key={field}>{field}<input required type={field==='date'?'date':field==='time'?'time':'text'} value={form[field]} onChange={(e)=>setForm({...form,[field]:e.target.value})}/></label>)}<button>Submit event</button></form><h2>Approved calendar</h2><select aria-label="Filter events by category" value={category} onChange={(e)=>setCategory(e.target.value)}>{cats.map(c=><option key={c}>{c}</option>)}</select><div className="cards">{filtered.map(event=><article className="card" key={event.id}><time>{event.date} {event.time}</time><h3>{event.title}</h3><p>{event.organizer} · {event.venue}</p><p className="eyebrow">{event.category}</p></article>)}</div></section></Layout>}
